# ============================================================
# FRONTEND ENVIRONMENT CONFIG — Copy this file to .env
# and fill in your own values before running.
# ============================================================

# URL where your local Strapi backend is running
VITE_STRAPI_URL=http://localhost:1337

# Strapi API Token — generate one from:
# Strapi Admin → Settings → API Tokens → Create new token (Full Access)
VITE_STRAPI_API_TOKEN=your-strapi-api-token-here
